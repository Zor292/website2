# 🚀 دليل الرفع على Railway - خطوة بخطوة

---

## الخطوة 1: إعادة توليد Client Secret (مهم جداً ⚠️)

1. افتح https://discord.com/developers/applications
2. اختر تطبيقك
3. اضغط على **OAuth2** من القائمة
4. اضغط **Reset Secret** واحفظ الـ Secret الجديد

---

## الخطوة 2: رفع المشروع على GitHub

1. افتح https://github.com وسجّل دخول (أو أنشئ حساب)
2. اضغط **New Repository**
3. اسمه: `discord-auth-app`
4. اجعله **Private** (مهم للأمان)
5. ارفع كل ملفات المجلد عليه

---

## الخطوة 3: النشر على Railway

1. افتح https://railway.app
2. سجّل دخول بحساب GitHub
3. اضغط **New Project** ← **Deploy from GitHub repo**
4. اختر الـ repo الذي رفعته
5. Railway سيبدأ البناء تلقائياً ✅

---

## الخطوة 4: إضافة Environment Variables على Railway

في لوحة Railway اضغط على مشروعك ← **Variables** ثم أضف:

| المتغير | القيمة |
|---------|--------|
| `CLIENT_ID` | `1476983875598024824` |
| `CLIENT_SECRET` | (الـ secret الجديد من الخطوة 1) |
| `SESSION_SECRET` | (اكتب أي نص عشوائي طويل) |
| `REDIRECT_URI` | (اتركه فارغاً الآن، سنضيفه بعد ما تعرف الرابط) |
| `NODE_ENV` | `production` |

---

## الخطوة 5: معرفة رابط موقعك

1. في Railway اضغط على **Settings** ← **Domains**
2. اضغط **Generate Domain**
3. ستحصل على رابط مثل: `https://discord-auth-app-xxxx.up.railway.app`
4. **انسخ هذا الرابط**

---

## الخطوة 6: تحديث REDIRECT_URI

1. في Railway ← Variables، حدّث `REDIRECT_URI` إلى:
   ```
   https://YOUR-LINK.up.railway.app/auth/callback
   ```

2. في Discord Developer Portal ← OAuth2 ← **Redirects**، أضف نفس الرابط:
   ```
   https://YOUR-LINK.up.railway.app/auth/callback
   ```

3. اضغط **Save Changes**

---

## الخطوة 7: إعادة النشر

في Railway اضغط **Redeploy** ليأخذ المتغيرات الجديدة.

---

## ✅ انتهيت!

افتح رابط موقعك وجرب تسجيل الدخول بـ Discord!

---

## 🆘 مشاكل شائعة

| المشكلة | الحل |
|---------|------|
| `redirect_uri_mismatch` | تأكد أن REDIRECT_URI في Railway وDiscord متطابقان تماماً |
| `invalid_client` | تأكد أن CLIENT_SECRET صحيح |
| الموقع لا يفتح | انتظر دقيقتين بعد النشر |
